# OpenWeatherMap Configuration
OPENWEATHER_API_KEY=<REPLACE>
OPENWEATHER_BASE_URL=<REPLACE>

# Server Configuration
PORT=<REPLACE>
NODE_ENV=<REPLACE>

# MongoDB Configuration
MONGODB_URI=<REPLACE>

# Twilio Configuration (Placeholder values for testing)
TWILIO_ACCOUNT_SID=<REPLACE>
TWILIO_AUTH_TOKEN=<REPLACE>
TWILIO_PHONE_NUMBER=<REPLACE>

# Email Configuration
EMAIL_HOST=<REPLACE>
EMAIL_PORT=<REPLACE>
EMAIL_USER=<REPLACE>
EMAIL_PASS=<REPLACE>
EMAIL_FROM=<REPLACE>

